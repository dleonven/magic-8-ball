Running the app:

Choose a directory and in the terminal run:

1. git clone https://github.com/dleonven/magic-8-ball.git
2. cd magic-8-ball
3. yarn
4. yarn dev

Now the app should be running on http://localhost:3000, and the magic 8 ball
should be ready to answer ;)